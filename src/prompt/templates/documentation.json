{
  "id": "documentation",
  "name": "Documentation Generation",
  "template": {
    "instructions": "Generate ${documentationType} documentation for this ${language} code (target: ${targetAudience}).\n\n```${language}\n${selectedCode}\n```\n\nInclude:\n1. Overview and purpose\n2. Parameters/arguments with types\n3. Return values\n4. Usage examples\n5. Error handling\n\nUse ${language} documentation conventions (JSDoc, docstrings, etc.).",
    "variables": [
      "language",
      "selectedCode",
      "documentationType",
      "targetAudience"
    ]
  },
  "config": {
    "default_values": {
      "documentationType": "api",
      "targetAudience": "developers"
    }
  }
}
