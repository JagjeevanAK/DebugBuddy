{
  "id": "performance-analysis",
  "name": "Performance Analysis",
  "template": {
    "instructions": "Analyze this ${language} code for performance issues. Focus: ${performanceFocus}.\n\n```${language}\n${selectedCode}\n```\n\nFor each issue:\n**[IMPACT]** Issue\n- Current behavior and complexity (time/space)\n- Bottleneck cause\n- Optimization with before/after code\n- Expected improvement\n- Trade-offs\n\nImpact levels: CRITICAL (unusable), HIGH (UX affected), MEDIUM (recommended), LOW (minor).\n\nTarget metrics: ${targetMetrics}. Apply ${language}-specific optimizations.",
    "variables": [
      "language",
      "selectedCode",
      "performanceFocus",
      "targetMetrics"
    ]
  },
  "config": {
    "default_values": {
      "performance_focus": "cpu, memory, io",
      "target_metrics": "latency, throughput, memory_usage"
    }
  }
}
